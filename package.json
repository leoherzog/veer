{
  "name": "veer",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "wrangler types && npm run build:frontend && wrangler d1 migrations apply veer-db --local && node frontend/esbuild.mjs --watch & wrangler dev",
    "build:frontend": "node frontend/esbuild.mjs --minify",
    "build": "wrangler types && npm run build:frontend",
    "deploy": "npm run build && wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply veer-db --local",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@awesome.me/webawesome": "^3.3.1",
    "@better-auth/passkey": "^1.5.5",
    "better-auth": "^1.5.4",
    "chart.js": "^4.5.1",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.12.5"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.2",
    "drizzle-kit": "^0.31.9",
    "esbuild": "^0.27.3",
    "typescript": "^6.0.2",
    "vitest": "^4.1.0",
    "wrangler": "^4.76.0"
  }
}
