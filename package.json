{
  "name": "veer",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "dev": "npm run build:frontend && wrangler d1 migrations apply veer-db --local && node frontend/esbuild.mjs --watch & wrangler dev",
    "build:frontend": "node frontend/esbuild.mjs",
    "build": "npm run build:frontend",
    "deploy": "npm run build && wrangler deploy",
    "db:generate": "drizzle-kit generate",
    "db:migrate:local": "wrangler d1 migrations apply veer-db --local",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@better-auth/passkey": "^1.5.5",
    "@web.awesome.me/webawesome-pro": "^3.3.1",
    "better-auth": "^1.5.4",
    "drizzle-orm": "^0.45.1",
    "hono": "^4.12.5"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.13.2",
    "@vitest/runner": "^4.1.0",
    "@vitest/snapshot": "^4.1.0",
    "drizzle-kit": "^0.31.9",
    "esbuild": "^0.27.3",
    "typescript": "^5.9.3",
    "vitest": "^4.1.0",
    "wrangler": "^4.74.0"
  }
}
